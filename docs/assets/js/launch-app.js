(()=>{
const DATA={cfd_prop_firm:'cfd-prop-firms.json',futures_prop_firm:'futures-prop-firms.json',crypto_prop_firm:'crypto-prop-firms.json',broker:'brokers.json',crypto_exchange:'crypto-exchanges.json'};
const META={cfd_prop_firm:{label:'CFD Prop Firms',path:'cfd-prop-firms',desc:'تحديات، حسابات، أسعار، دروداون، سحب، قواعد ومخاطر.'},futures_prop_firm:{label:'Futures Prop Firms',path:'futures-prop-firms',desc:'رسوم شهرية، عقود، Trailing DD، سحب ورسوم إضافية.'},crypto_prop_firm:{label:'Crypto Prop Firms',path:'crypto-prop-firms',desc:'كريبتو، رافعة، API، KYC، تصفية وسحب.'},broker:{label:'Brokers',path:'brokers',desc:'تنظيم، حسابات، سبريد، عمولات، منصات، إيداع وسحب.'},crypto_exchange:{label:'Crypto Exchanges',path:'crypto-exchanges',desc:'رسوم، KYC، P2P، Futures، أمان وقيود دول.'}};
const base='/Compari-prop-firm/',$=(s,r=document)=>r.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),miss=v=>v==null||v===''||(Array.isArray(v)&&!v.length),fmt=v=>window.ProTableEngine?window.ProTableEngine.format(v):(miss(v)?'—':esc(Array.isArray(v)?v.join('، '):v)),abbr=n=>String(n||'?').split(/\s+/).filter(Boolean).slice(0,3).map(x=>x[0]).join('').toUpperCase(),status=s=>`<span class="badge ${s==='ready'?'ready':s==='partial'?'partial':'info'}">${fmt(s)}</span>`;
let STORE={index:[],detail:{}};
async function getJson(p){const r=await fetch(base+p,{cache:'no-store'});if(!r.ok)throw Error(p);return r.json()}
async function load(){STORE.index=await getJson('src/data/index.json');await Promise.all(Object.entries(DATA).map(async([cat,file])=>{try{STORE.detail[cat]=await getJson('src/data/'+file)}catch(e){STORE.detail[cat]=[]}}));route()}
function byCat(cat){const d=STORE.detail[cat]||[],idx=STORE.index.filter(x=>x.category===cat);return d.length?d:idx}
function allRows(){const details=Object.values(STORE.detail).flat();return details.length?details:STORE.index}
function findCompany(cat,slug){const d=(STORE.detail[cat]||[]).find(x=>x.slug===slug||x.company_name===slug);if(d)return d;const i=STORE.index.find(x=>(!cat||x.category===cat)&&(x.slug===slug||x.company_name===slug));return i||byCat(cat)[0]||STORE.index[0]}
function firstPlan(c){return(c.programs||[]).flatMap(p=>p.plans||[])[0]||{}}
function cell(c){return`<div class="companyCell"><span class="abbr">${esc(abbr(c.company_name))}</span><div><b>${esc(c.company_name)}</b><small>${fmt(c.company_type||c.category_label||c.category)}</small></div></div>`}
function layout(inner){document.body.innerHTML=`<header class="top"><div class="wrap nav"><a class="brand" href="${base}"><span class="mark">م</span><span>محطة التمويل</span></a><nav class="links"><a href="${base}">الرئيسية</a><a href="${base}categories/">التصنيفات</a><a href="${base}review/">المراجعة</a><a href="${base}compare/">المقارنة</a></nav></div></header>${inner}<footer class="footer"><div class="wrap"><h3>محطة التمويل</h3><p>مرجع عربي للمراجعة والمقارنة. تحقق من المصدر الرسمي قبل أي قرار مالي.</p></div></footer>`}
function route(){const p=location.pathname.replace(base,'').replace(/^\//,'');if(p.startsWith('review'))return review();if(p.startsWith('compare'))return compare();if(p.startsWith('categories'))return categories();if(p.startsWith('category/'))return category(p.split('/')[1]);home()}
function home(){const ready=STORE.index.filter(c=>c.data_quality_status==='ready').length;layout(`<main><section class="hero"><div class="wrap heroGrid"><div><span class="kicker">مرجع المتداول المحترف</span><h1>راجع الشركة بعمق وقارن الحسابات قبل أن تدفع</h1><p class="lead">مراجعات وجداول للبرامج، الحسابات، الأسعار، قواعد التداول، التراجع، السحب، المنصات والمصادر.</p><div class="actions"><a class="btn primary" href="${base}review/">افتح مراجعة شركة</a><a class="btn" href="${base}compare/">قارن شركات أو حسابات</a></div></div><div class="card"><h2>بحث سريع</h2><input id="q" placeholder="FTMO, Topstep, Binance..."><div id="searchResults" class="grid"></div><div class="statRow"><span class="badge ready">${ready} Ready</span><span class="badge info">${STORE.index.length} شركة في الفهرس</span><span class="badge info">${allRows().length} شركة تفصيلية</span></div></div></div></section><section class="section"><div class="wrap"><div class="head"><h2>التصنيفات</h2><span class="muted">كل نوع له جداوله الخاصة</span></div><div class="grid grid5">${Object.entries(META).map(([k,m])=>`<a class="card catCard" href="${base}category/${m.path}/"><strong>${m.label}</strong><p>${m.desc}</p><span class="badge info">${byCat(k).length} شركة</span></a>`).join('')}</div></div></section><section class="section"><div class="wrap"><div class="head"><h2>كل الشركات من قاعدة البيانات</h2><span class="muted">يعرض جميع شركات الفهرس، وليس أول 30 فقط</span></div>${homeTable(STORE.index)}</div></section></main>`);$('#q')?.addEventListener('input',e=>{const q=e.target.value.trim().toLowerCase();$('#searchResults').innerHTML=q.length<2?'':STORE.index.filter(c=>c.company_name.toLowerCase().includes(q)).slice(0,15).map(c=>`<a class="card" href="${base}review/?category=${c.category}&company=${c.slug}">${cell(c)}<span class="badge info">Review</span></a>`).join('')})}
function homeTable(rows){return`<div class="tableBox"><table><thead><tr><th class="sticky">الشركة</th><th>النوع</th><th>أقل سعر/إيداع</th><th>أكبر حساب</th><th>منصات</th><th>حالة</th><th>آخر تحقق</th><th>فتح</th></tr></thead><tbody>${rows.map(c=>`<tr><td class="sticky">${cell(c)}</td><td>${fmt(c.category_label||c.category)}</td><td>${fmt(c.lowest_price_or_deposit)}</td><td>${fmt(c.max_account_size)}</td><td>${fmt(c.platforms)}</td><td>${status(c.data_quality_status)}</td><td>${fmt(c.last_checked)}</td><td><a class="btn" href="${base}review/?category=${c.category}&company=${c.slug}">Review</a></td></tr>`).join('')}</tbody></table></div>`}
function categories(){layout(`<main><section class="hero"><div class="wrap"><span class="kicker">التصنيفات</span><h1>اختر نوع السوق</h1><p class="lead">كل تصنيف يعرض الشركات المرتبطة به من قاعدة البيانات.</p></div></section><section class="section"><div class="wrap grid grid5">${Object.entries(META).map(([k,m])=>`<a class="card catCard" href="${base}category/${m.path}/"><strong>${m.label}</strong><p>${m.desc}</p><span class="badge info">${byCat(k).length} شركة</span></a>`).join('')}</div></section></main>`)}
function catByPath(p){return Object.keys(META).find(k=>META[k].path===p)}
function category(path){const cat=catByPath(path)||'cfd_prop_firm',list=byCat(cat);layout(`<main><section class="hero"><div class="wrap"><span class="kicker">${META[cat].label}</span><h1>كل الشركات داخل التصنيف</h1><p class="lead">يظهر هنا كل ما هو موجود في قاعدة البيانات لهذا النوع.</p></div></section><section class="section"><div class="wrap">${categoryTable(cat,list)}</div></section></main>`)}
function categoryTable(cat,list){const rows=list.map(c=>({c,p:firstPlan(c)}));const cols=cat==='broker'?[['الشركة',r=>cell(r.c)],['Min Deposit',r=>fmt(r.c.broker_specific?.minimum_deposit||r.c.lowest_price_or_deposit||r.p.price)],['Account Types',r=>fmt(r.c.broker_specific?.account_types)],['Platforms',r=>fmt(r.c.markets_and_platforms?.platforms||r.c.platforms)],['Regulators',r=>fmt(r.c.broker_specific?.regulators)],['Islamic',r=>fmt(r.c.broker_specific?.islamic_account)],['Review',r=>`<a class="btn" href="${base}review/?category=${cat}&company=${r.c.slug}">فتح</a>`]]:cat==='crypto_exchange'?[['المنصة',r=>cell(r.c)],['Spot',r=>fmt(r.c.crypto_exchange_specific?.spot_trading)],['Futures',r=>fmt(r.c.crypto_exchange_specific?.futures_trading)],['P2P',r=>fmt(r.c.crypto_exchange_specific?.p2p_trading)],['Maker/Taker',r=>`${fmt(r.c.crypto_exchange_specific?.maker_fee)} / ${fmt(r.c.crypto_exchange_specific?.taker_fee)}`],['KYC',r=>fmt(r.c.crypto_exchange_specific?.kyc_required)],['Review',r=>`<a class="btn" href="${base}review/?category=${cat}&company=${r.c.slug}">فتح</a>`]]:[['الشركة',r=>cell(r.c)],['الحساب',r=>fmt(r.p.account_size||r.c.max_account_size)],['السعر',r=>fmt(r.p.price||r.c.lowest_price_or_deposit)],['P1/P2',r=>`${fmt(r.p.phase_1_profit_target)} / ${fmt(r.p.phase_2_profit_target)}`],['Daily/Max DD',r=>`${fmt(r.p.daily_drawdown)} / ${fmt(r.p.max_drawdown)}`],['DD Type',r=>fmt(r.p.drawdown_type)],['Payout',r=>fmt(r.p.first_payout_time||r.p.payout_frequency)],['Platforms',r=>fmt(r.c.markets_and_platforms?.platforms||r.c.platforms)],['Review',r=>`<a class="btn" href="${base}review/?category=${cat}&company=${r.c.slug}">فتح</a>`]];return`<div class="note">عدد الشركات المعروضة: ${list.length}</div><div class="tableBox"><table><thead><tr>${cols.map((c,i)=>`<th class="${i===0?'sticky':''}">${c[0]}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>`<tr>${cols.map((c,i)=>`<td class="${i===0?'sticky':''}">${c[1](r)}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`}
function review(){const qs=new URLSearchParams(location.search),cat=qs.get('category')||'cfd_prop_firm',slug=qs.get('company'),company=findCompany(cat,slug);const selector=`<div class="card"><div class="compareControls"><select id="catSel">${Object.entries(META).map(([k,m])=>`<option value="${k}" ${k===cat?'selected':''}>${m.label}</option>`).join('')}</select><select id="companySel"></select></div></div>`;layout(`<main><section class="hero"><div class="wrap"><span class="kicker">Review Engine</span><h1>مراجعة شاملة لكل برنامج وحساب</h1><p class="lead">تعرض المتوفر فقط، وتخفي الأعمدة الفارغة.</p>${selector}</div></section><section class="section"><div class="wrap" id="reviewHost"></div></section></main>`);const fill=()=>{const list=byCat($('#catSel').value);$('#companySel').innerHTML=list.map(c=>`<option value="${c.slug}" ${company?.slug===c.slug?'selected':''}>${c.company_name}</option>`).join('')};const draw=()=>{const c=findCompany($('#catSel').value,$('#companySel').value);$('#reviewHost').innerHTML=window.ProTableEngine.renderReview(c);window.ProTableEngine.initTabs($('#reviewHost'));history.replaceState(null,'',`${base}review/?category=${$('#catSel').value}&company=${c.slug}`)};fill();if(slug)$('#companySel').value=company?.slug||$('#companySel').value;$('#catSel').onchange=()=>{fill();draw()};$('#companySel').onchange=draw;draw()}
function compare(){layout(`<main><section class="hero"><div class="wrap"><span class="kicker">Compare Engine</span><h1>مقارنة شركات أو حسابات</h1><p class="lead">قارن حسب السعر، الدرو داون، القواعد أو السحب.</p><div class="card compareControls"><select id="cmpCat">${Object.entries(META).map(([k,m])=>`<option value="${k}">${m.label}</option>`).join('')}</select><select id="a"></select><select id="b"></select><select id="c"></select><select id="axis"><option value="core">Core Decision</option><option value="rules">Trading Rules</option><option value="payout">Payout</option></select></div></div></section><section class="section"><div class="wrap" id="cmpOut"></div></section></main>`);const fill=()=>{const list=byCat($('#cmpCat').value);['a','b','c'].forEach((id,idx)=>$('#'+id).innerHTML=list.map((x,i)=>`<option value="${x.slug}" ${i===idx?'selected':''}>${x.company_name}</option>`).join(''))};const draw=()=>{$('#cmpOut').innerHTML=compareTable(['a','b','c'].map(id=>findCompany($('#cmpCat').value,$('#'+id).value)).filter(Boolean),$('#axis').value)};fill();draw();['cmpCat','a','b','c','axis'].forEach(id=>$('#'+id).onchange=()=>{if(id==='cmpCat')fill();draw()})}
function compareTable(companies,axis){const rows=axis==='rules'?[['News',c=>firstPlan(c).news_trading],['Weekend',c=>firstPlan(c).weekend_holding],['EA/Bots',c=>firstPlan(c).ea_bots],['Copy',c=>firstPlan(c).copy_trading],['Hedging',c=>firstPlan(c).hedging],['HFT',c=>firstPlan(c).high_frequency_trading]]:axis==='payout'?[['Profit Split',c=>firstPlan(c).profit_split],['First Payout',c=>firstPlan(c).first_payout_time],['Frequency',c=>firstPlan(c).payout_frequency],['Refund',c=>firstPlan(c).refund_policy],['Payout Methods',c=>c.funded_stage_and_payouts?.payout_methods],['KYC Before Payout',c=>c.funded_stage_and_payouts?.kyc_before_payout]]:[['Account Size',c=>firstPlan(c).account_size||c.max_account_size],['Price',c=>firstPlan(c).price||c.lowest_price_or_deposit],['Phase 1 Target',c=>firstPlan(c).phase_1_profit_target],['Phase 2 Target',c=>firstPlan(c).phase_2_profit_target],['Daily DD',c=>firstPlan(c).daily_drawdown],['Max DD',c=>firstPlan(c).max_drawdown],['DD Type',c=>firstPlan(c).drawdown_type],['Platforms',c=>c.markets_and_platforms?.platforms||c.platforms],['Source',c=>firstPlan(c).official_sources||c.official_website]];return`<div class="tableBox"><table><thead><tr><th class="sticky">المعيار</th>${companies.map(c=>`<th><span class="abbr">${esc(abbr(c.company_name))}</span><br>${esc(c.company_name)}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>`<tr><td class="sticky"><b>${r[0]}</b></td>${companies.map(c=>`<td>${fmt(r[1](c))}</td>`).join('')}</tr>`).join('')}</tbody></table></div>${companies.map(c=>`<a class="btn" href="${base}review/?category=${c.category}&company=${c.slug}">مراجعة ${esc(c.company_name)}</a> `).join('')}`}
document.addEventListener('DOMContentLoaded',()=>load().catch(e=>{console.error(e);document.body.innerHTML='<main class="wrap section"><div class="note">فشل تحميل قاعدة البيانات.</div></main>'}));
})();
